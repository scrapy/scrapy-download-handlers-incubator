name: Ubuntu

permissions:
  contents: read

on:
  push:
    branches:
    - main
    - '[0-9]+.[0-9]+'
  pull_request:

concurrency:
  group: ${{github.workflow}}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  tests:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      PYTEST_ADDOPTS: -n auto
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
        env:
          - TOXENV: py-all
        include:
          - python-version: '3.10'
            env:
              TOXENV: py-pinned
          - python-version: '3.10'
            env:
              TOXENV: py-pinned-noreactor
          - python-version: '3.13'
            env:
              TOXENV: py-all-noreactor
          - python-version: '3.13'
            env:
              TOXENV: py-aiohttp
          - python-version: '3.13'
            env:
              TOXENV: py-curlcffi
          - python-version: '3.13'
            env:
              TOXENV: py-httpx
          - python-version: '3.13'
            env:
              TOXENV: py-niquests
          - python-version: '3.13'
            env:
              TOXENV: py-pyreqwest

    steps:
    - uses: actions/checkout@v6
      with:
        persist-credentials: false

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}

    - name: Run tests
      env: ${{ matrix.env }}
      run: |
        pip install -U tox
        tox

    - name: Upload coverage report
      uses: codecov/codecov-action@v6

    - name: Upload test results
      if: ${{ !cancelled() }}
      uses: codecov/codecov-action@v6
      with:
        report_type: test_results
